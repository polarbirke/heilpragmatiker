<!doctype html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Lasst euch impfen! – Heilpragmatiker</title>
    <style>
        * {
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background: {{ covidData.incidenceColor }};
            color: {{ covidData.textColor }};
        }

        main {
            display: grid;
            grid-template-rows: 1fr 2rem;
            min-height: 100vh;
            padding: 2rem;
        }

        h1 {
            align-self: center;
            font-size: 4rem;
            font-size: clamp(3.5rem, calc(7vw + 1rem), 10rem);
            line-height: 1.125;
            text-align: center;
        }

        a {
            color: inherit;
        }

        .meta {
            align-self: end;
            text-align: center;
        }
    </style>
</head>
<body>
<main>
    <h1 style="color: {{ covidData.text }};">Lasst euch impfen!</h1>
    <div class="meta">
        <p>Aktuelle 7-Tage-Inzidenz: {{ covidData.weekIncidence }}</p>
        {% if covidData.lastUpdate %}<p><small>(Quelle: <a href="https://corona.rki.de">RKI</a>; letztes Update {{ covidData.lastUpdate|readableDate }})</small></p>{% endif %}
    </div>
</main>
</body>
</html>
