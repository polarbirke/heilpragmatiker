<!doctype html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ meta.site.messsage }} – {{ meta.site.name }}</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' version='1.1' xmlns:xlink='http://www.w3.org/1999/xlink' width='1000' height='1000'%3E%3Cg%3E%3Crect x='0' y='0' width='1000' height='1000' fill='%23{{ covidData.incidenceColor }}'%3E%3C/rect%3E%3Ctext x='50%25' y='65%25' alignment-baseline='central' text-anchor='middle' font-family='monospace' font-size='450' fill='%23{{ covidData.textColor }}'%3E{% if covidData.weekIncidenceInteger %}{{ covidData.weekIncidenceInteger }}{% endif %}%3C/text%3E%3C/g%3E%3C/svg%3E%0A">
    <link rel="canonical" href="{{ meta.site.url }}">
    <meta property="og:title" content="{{ meta.site.name }}">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="{{ meta.site.name }}">
    <meta property="og:url" content="{{ meta.site.url }}">
    <meta property="og:description" content="{{ meta.site.message }} Aktuelle 7-Tage-Inzidenz in Deutschland: {{ covidData.weekIncidence }}">
    <meta property="og:image" content="https://agitated-lewin-cc3724.netlify.app/https%3A%2F%2Fwww.heilpragmatiker.de%2F/opengraph/">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:description" content="{{ meta.site.message }} Aktuelle 7-Tage-Inzidenz in Deutschland: {{ covidData.weekIncidence }}">
    <meta name="twitter:image" content="https://agitated-lewin-cc3724.netlify.app/https%3A%2F%2Fwww.heilpragmatiker.de%2F/opengraph/{{ covidData.lastUpdate }}">
    <style>
        * {
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Avenir Next', Avenir, 'Segoe UI', 'Helvetica Neue', Ubuntu, Roboto, noto, arial, sans-serif;
            background: #{{ covidData.incidenceColor }};
            color: #{{ covidData.textColor }};
        }

        main {
            display: grid;
            grid-template-rows: 1fr 2rem;
            min-height: 100vh;
            padding: 2rem;
        }

        h1 {
            align-self: center;
            font-size: 4rem;
            font-size: clamp(3.5rem, calc(7vw + 1rem), 10rem);
            line-height: 1.125;
            text-align: center;
        }

        a {
            color: inherit;
        }

        .meta {
            align-self: end;
            text-align: center;
        }

        .mt-100 {
            margin-top: 0.25rem;
        }

        .opacity-80 {
            opacity: 0.8;
        }
    </style>
</head>
<body>
<main>
    <h1>Lasst euch impfen!</h1>
    {% if covidData.weekIncidence %}
    <div class="meta">
        <p>Aktuelle 7-Tage-Inzidenz in Deutschland: {{ covidData.weekIncidence }}</p>
        {% if covidData.lastUpdate %}<p class="mt-100 opacity-80"><small>(Quelle: <a href="https://corona.rki.de">RKI</a>; Stand der Daten: {{ covidData.lastUpdate|readableDate }} Uhr)</small></p>{% endif %}
    </div>
    {% endif %}
</main>
</body>
</html>
